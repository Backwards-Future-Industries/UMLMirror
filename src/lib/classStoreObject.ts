class classStoreObject {

    title: string;
    atributes: string[];
    methods: string[];
    x: number;
    y: number;
    
    constructor(title: string, atributes: string[], methods: string[], x: number, y: number) {
        this.title = title;
        this.atributes = atributes;
        this.methods = methods;
        this.x = x;
        this.y = y;
    }
}